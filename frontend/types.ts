/**
 * React and React-DOM’s npm packages include standalone .js files that you can include in a web page,
 * and we’re referencing them directly to get things moving faster, thus referencing them in browser.
 *
 * Alternatively, feel free to host these files on a content delivery network (CDN)
 */

import "../node_modules/react/umd/react.development.js";
import "../node_modules/react-dom/umd/react-dom.development.js";
